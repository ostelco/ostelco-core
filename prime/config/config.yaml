services:
  - type: api

eventProcessor:
  configFile: /secret/pantel-prod.json
  projectId: pantel-2decb
  topicId: data-traffic

logging:
  level: INFO
  loggers:
    org.ostelco: DEBUG
  appenders:
    - type: console
      layout:
        type: json

